#use("Object.kj");

class Person
{
	let name;
	let age;

	met Constructor(n, a)
	{
		#ensureType(n, String);
		#ensureType(a, Int);

		this.name = n;
		this.age = a;
	}

	met Destructor()
	{
	}
}
